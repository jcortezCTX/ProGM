node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Local-disk attachment storage (see src/lib/attachmentStorage.ts) - not
# meant to be committed, same as any other uploaded user content.
/uploads
